# Brand assets

- `mascot.svg` — **PLACEHOLDER** for the official CMVNG green "dripping"
  character. It is an original stand-in drawn for layout purposes only.
  Drop the real artwork here with the same filename (SVG preferred; a PNG
  works if you also update the import extension in `src/components/ui.jsx`).
  Per DESIGN.md, the character may appear ONLY as: share-card avatar,
  about/intro moment, loading/empty states, small mascot corner on the plan
  share card. Never as a background, never behind text or data, never
  recolored or stretched.
- The logo bars mark is drawn inline as SVG in `src/components/ui.jsx`
  (`LogoMark`) using the locked blue family. If you have the official mark
  file, add it here and swap the component's contents.
