// CMVNG simulation methodology version.
// Bump MINOR when a calculation changes in a way that alters results;
// bump MAJOR when the methodology itself is redefined.
// Saved plans store the version that produced them — never reinterpret old
// results with a newer model silently.
export const MODEL_VERSION = "3.0.0";
export const MODEL_LABEL = `CMVNG Simulation v${MODEL_VERSION}`;
